# Preserve all append-only log entries across concurrent branch updates
logs/*.log merge=union
logs/*.csv merge=union
logs/*.txt merge=union

# Preserve continuous trade memory and state tracking across bot runs
data/*.json merge=union
data/*.csv merge=union
